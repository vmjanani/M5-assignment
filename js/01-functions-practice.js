//STEP 1
/*
Practice with Functions (5 points)

Write a function called halfNumber() that accepts one argument (a number), divide it by 2, and return the result. It should log a string like "Half of 5 is 2.5.".
*/
/*

let inputNumber =  parseInt(prompt('Enter a number:'));
halfnumber(inputNumber);

function halfnumber(){

    let half= inputNumber/2;
    return console.log('Half of ' + inputNumber + 'is ' + half)
}

*/



//STEP 2
//Write a function called squareNumber() that accepts one argument (a number), square that number, and return the result. It should also log a string like "The result of squaring the number 3 is 9."

// let startNumber = parseInt(prompt('Enter a number:'));

// squareNumber(startNumber);

// function squareNumber(){

//      let squared = startNumber*startNumber;
//      return console.log ('The result of squaring the number ' + startNumber + ' is ' + squared + '.');

// };

//STEP 3
//Write a function called percentOf() that accepts two numbers, figure out what percent the first number represents of the second number, and return the result. It should log a string like "2 is 50% of 4."

// let startNumber01 = parseInt(prompt('Enter your first number:'));
// let startNumber02 = parseInt(prompt('Enter your second number:'));

// percentOf(startNumber01, startNumber02);

// function percentOf(one, two){

//      let percent = (one/two)*100;
//      return console.log (startNumber01 + ' is ' + percent + '% of ' + startNumber02 + '.');

// };


//STEP 4
//Write a function called findModulus() that accepts two numbers. Within the function write a statement that returns the modulus of the first and second parameters. It should log a string like "2 is the modulus of 4 and 10."

// let startNumber01 = parseInt(prompt('Enter your first number:'));
// let startNumber02 = parseInt(prompt('Enter your second number:'));

// findModulus(startNumber01, startNumber02);

// function findModulus(one, two){

//      let modulus = (one % two);
//      return console.log (modulus + ' is the modulus of ' + startNumber01 + ' and ' + startNumber02 + '.');

// };